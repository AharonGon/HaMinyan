<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.HaMinyan" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- מסך מערכת קצר בלבד; התמונה המלאה (splash.png) מוצגת ב-Compose -->
    <style name="Theme.HaMinyan.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground_inset</item>
        <item name="postSplashScreenTheme">@style/Theme.HaMinyan</item>
    </style>
</resources>
